# Subtrace

A simple subdomain enumeration tool inspired by Amass.  
It queries **crt.sh** for certificate transparency logs to find subdomains of a given domain.

---

## Usage
```bash
python subtrace.py
